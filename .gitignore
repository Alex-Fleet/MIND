# ───────────────────────────────────────────────
# 🔒 私密运行时数据 —— 绝不进版本控制 / 分享包
# 这是你全部项目的对话史与记忆，别人拿到的应是"系统"而非"你的记忆"
# ───────────────────────────────────────────────
data/
*.db
*.db-journal
*.db-wal

# ── 密钥 / 本地覆盖配置 ──
config.local.json
config.json
.env
*.key

# ── CLAUDE.md — MIND 生成，含用户个人铁律 ──
CLAUDE.md

# ── 用户私有的项目注册表（slug 分身映射 + 标签），出厂应为空 ──
projects.json
projects.draft.json

# ── Python ──
__pycache__/
*.pyc
*.py[cod]

# ── Claude Code 工作区 ──
.claude/

# ── 诊断 / 日志 ──
hook_heartbeat.log
*.log

# ── 备份 / OS ──
*.bak
.DS_Store
